#include <bits/stdc++.h>
using namespace std;
//Functions
    string solve(string s){
        for(int i=0;i<s.length();i++){
            if(s[i]==('a',)
        }
    }

int main(){
    string s;
    getline(cin,s);
    string ans=solve(s);

return 0;
}