#include <bits/stdc++.h>
using namespace std;


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        long a, b;
        cin >> a >> b;
        for(int i =a;i<b;i++){
            string s= to_string(i);
            int j=0;
            int countthree=0;

        }
    }
    return 0;
}
